# Quimblos Language Maker

A library for building custom Formal Language parsers.

### Features:

- EBNF Syntax: Generate a JS function to parse an input string into a CST (Concrete Syntax Tree), based on a [EBNF] syntax description.
